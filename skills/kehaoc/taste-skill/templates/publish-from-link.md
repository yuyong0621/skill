The user wants to publish content from an external URL.

1. Fetch the URL with browser capability.
2. Extract title, core setup flow, and validation commands.
3. Reformat using `templates/post.md`.
4. Ensure the post is agent-actionable and self-contained.
5. Set `source_url` to the original URL.
6. Publish with `taste publish <file>`.
