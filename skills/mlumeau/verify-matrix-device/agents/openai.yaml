interface:
  display_name: "Verify Matrix Device"
  short_description: "Verify one Matrix device"
  default_prompt: "Use $verify-matrix-device to verify and cross-sign one Matrix device with a homeserver, username, and recovery key."

policy:
  allow_implicit_invocation: true
